{
  "name": "SpotiKai",
  "version": "1.0.0",
  "description": "Spotify streaming client for KaiOS",
  "author": "",
  "repository": "https://github.com/hkgumbs/spotikai",
  "license": "UNLICENSED",
  "scripts": {
    "start": "MODE=release node src/index.js",
    "start:debug": "rerun -- MODE=debug PORT=4001 node src/index.js",
    "postinstall": "(cd librespot && git init && git reset --hard && git apply ../src/librespot.patch)"
  },
  "dependencies": {
    "@suldashi/lame": "^1.2.5",
    "cookies": "^0.8.0",
    "node-fetch": "^2.6.0"
  }
}
